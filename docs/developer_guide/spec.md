# 仕様概要（MVP）

## 目的
友達同士で集まったときに、採点対象（例: M-1の出場者）を各自が採点し、
- 全員合計の合計点と順位
- 各参加者ごとの順位
をスマホから確認できる。

## 用語
- **イベント**: みんなで採点する会（例: M-1 2025 決勝）
- **採点対象**: イベント内で採点される対象（例: 出場者）
- **参加者**: スマホからアクセスして採点する人

## 画面（最小）
- イベント作成（タイトル、採点対象のリスト）
- 参加（イベントID、名前）
- 採点（各対象に0〜100点）
- 結果表示
  - 全員合計: 対象ごとの合計点と順位
  - 参加者別: 対象ごとの点数と順位

## 採点ルール
- 点数は整数の 0〜100。
- 未入力は 0 点として扱う。

## 順位ルール
- 高得点ほど上位。
- 同点は同順位。
- 次の順位は1つだけ進める（Dense Ranking: 1,1,2）。

## API（概要）
- `POST /api/events`: イベント作成
- `POST /api/events/{event_id}/join`: 参加
- `PUT /api/events/{event_id}/participants/{participant_id}/scores`: 採点保存
- `GET /api/events/{event_id}/results`: 結果取得
